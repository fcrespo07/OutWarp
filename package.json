{
  "name": "outwarp-ui-tests",
  "private": true,
  "description": "Dev-only tooling for the OutWarp UIs: esbuild for scripts/build_ui.py and the vitest suite in ui-tests/. Nothing here ships.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build:ui": "python scripts/build_ui.py"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "vitest": "5.0.2"
  }
}
